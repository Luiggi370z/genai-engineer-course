# Release evidence — the deployed assistant, measured

Generated by `make release-evidence` on 2026-08-03 · red team: all attacks contained

> **Provenance — full fidelity.** Measured against the deployed stack: retrieval `qdrant`/`hybrid-rrf` with embedder `nomic-embed-text` and reranker `BAAI/bge-reranker-base`; composer `ollama` running `qwen3.5:9b`; judged by RAGAS `0.4.3` with `qwen3-coder:30b` at temperature 0; tokens **part estimated**: some exchanges were counted by the provider and some fell back to a word split; red team = all 58 rows of the Phase 6 versioned dataset, benign controls included. No component fell back — checked before the first question and again after the last one, and this lane refuses to publish either way. Measured against source `1a0898d9`; `release.yml` will not publish a release whose code answers to a different one.

## Eval scores (RAGAS 0.4.3 · qwen3-coder:30b)

```
slice             faithfulness  context_recall   rows
-----------------------------------------------------
abstention               1.000           1.000      2
core                     0.611           1.000      3
-----------------------------------------------------
OVERALL                  0.767           1.000      5
```

Scored by real RAGAS metrics over the deployed retrieval path. The `abstention` slice is still a string check, deliberately: asking a judge whether a refusal is a refusal adds cost and variance to the one question that has a right answer.

## Red team — the full dataset (v3)

| family | rows | contained | BREACHED |
|---|---|---|---|
| approval-bypass | 3 | 3 | 0 |
| direct | 7 | 7 | 0 |
| encoded | 6 | 6 | 0 |
| exfiltration | 4 | 4 | 0 |
| indirect | 6 | 6 | 0 |
| multilingual | 4 | 4 | 0 |
| mutated | 8 | 8 | 0 |
| pii | 4 | 4 | 0 |
| tool-output | 5 | 5 | 0 |

**47 attacks, 0 reached a gated tool. 11 benign controls, 0 wrongly refused.**

Both numbers, always. Containment alone is satisfied by a service that refuses every request, which is why the controls are one per detector: a filter cannot pass this table by being afraid.

## Latency (from the spans)

`assistant.pipeline` over 63 runs: P50 39.6 ms · P95 1518.4 ms · P99 6101.3 ms

| where the time went | total ms |
|---|---|
| tool.read_emails | 0.8 |
| tool.read_news | 0.1 |

The whole pipeline on the deployed tier — retrieval, tools and composition — so these are the numbers a user would feel, not the agent loop's share of them. The P99 is the number the CI gate budgets — the tail confesses before the mean does.

## Cost

Composer tier: `ollama`, priced against the `local` list: **$0.0000** for 615 tokens in and 235 out across 63 runs, **part estimated**: some exchanges were counted by the provider and some fell back to a word split. Self-hosted generation has no per-token invoice, so the honest number here is zero — but the tokens are metered either way, which is the difference between a cost gate and a comforting sentence. Point the composer at a paid API, set `ASSISTANT_PRICE_TIER`, and the same measurement becomes a bill the CI gate blocks on before it reaches the invoice.

## Version stamps

| what | stamp |
|---|---|
| model | `qwen3.5:9b` |
| prompt | `grounded-fbc5e0e3` |
| corpus | `corpus-3-81e3e9c7` |
| dataset | `golden-5-d7ef7fc7+probes-3` |
| judge | `ragas-0.4.3/qwen3-coder:30b` |
| redteam | `v3+rows-58` |
| source | `1a0898d9` |

Derived from the run, not typed next to it: `prompt` is a hash of the prompt builder's own source, `corpus` and `dataset` hash their inputs. Every gate in `phase8-deploy/02-ci` refuses a report missing any of these — a number whose provenance you cannot state is not evidence.

---

These numbers are `release-report.json sha256:b1f51bd3239d298417c9af14ee32ecb4d944e4e40b1ec68697c9f9b5c92ba581`. `release.yml` recomputes that digest over the committed JSON and refuses to publish a page bound to different numbers.
