# pnpm 11 reads build-script approvals from here, not package.json.
# Native toolchains (Rust/Go binaries) need their install scripts to run.
allowBuilds:
  "@biomejs/biome": true
  "@tailwindcss/oxide": true
  esbuild: true
