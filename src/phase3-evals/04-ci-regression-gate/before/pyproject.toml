[project]
name = "ci-regression-gate-before"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8,<9",     # the gate is pure logic over two JSON files — keep it that way
]

[dependency-groups]
dev = ["pyright>=1.1.380,<2", "ruff>=0.6,<1"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pyright]
typeCheckingMode = "basic"
include = ["src"]
reportMissingImports = "none"

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]
markers = ["integration: needs a judge model via Ollama (make test-integration)"]
