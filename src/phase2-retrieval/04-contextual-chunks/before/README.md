# 2.4 Contextual chunks

One cheap sentence per chunk, big recall payoff — and it costs $0 on a local model. Fill the TODOs in `src/contextual.py` using `langchain-text-splitters` and Ollama.

```bash
make test                 # offline (injected summarizer)
make test-integration     # real local model
```
