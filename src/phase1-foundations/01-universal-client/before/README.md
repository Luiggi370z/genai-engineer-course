# 1.1 Universal client

Build one client that reaches every provider behind a single function, so swapping
models is config, not code. Fill the TODOs in `src/client.py`; run `make test`.

The tests pass without any API keys — they check the *shape* of your abstraction.
