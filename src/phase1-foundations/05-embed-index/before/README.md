# 1.5 Embed & index

Embed chunks, index them in numpy, search by cosine. Try a different-words query (hit) and an exact ID (miss — foreshadows Phase 2 hybrid search). Fill the TODOs in `src/index.py`.
