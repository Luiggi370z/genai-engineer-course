# 1.3 Structured extraction — reference

Typed `Invoice` via Instructor; same code hosted or local. The client is injected, so
the offline tests exercise the real extraction path against a fake — plus
`extract_all` / `violation_rate`, the numbers the hosted-vs-laptop shoot-out compares.
