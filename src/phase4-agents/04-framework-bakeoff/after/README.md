# 3.4 Framework bakeoff — reference

The same agent as a mini LangGraph state machine, a typed Pydantic-style agent, and a CrewAI-style role crew. Offline miniatures that capture each defining idea.

**Verdict template:** LangGraph = durability/branching/HITL (production default); Pydantic AI = type-safe single agent, minimal ceremony; CrewAI = fast role-based multi-agent prototyping. Choose by your dominant constraint.
